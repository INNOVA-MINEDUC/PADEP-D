# Vacio a proposito: en desarrollo las llamadas van a /api y el proxy de Vite
# (ver vite.config.js) las manda al backend Express. Solo hace falta ponerle
# valor si el frontend acaba sirviendose desde un origen distinto que la API.
VITE_API_URL=
